package com.github.hobbitProg.dcm.unitTests.client.books.bookCatalog.service.modifyingBook

import org.scalatest.PropSpec

/**
  * Specification for trying to remove the ISBN of a book using the service
  * @author Kyle Cranmer
  * @since 0.2
  */
class RemovingBookISBNSpec
    extends PropSpec {
  property("indicates the catalog was not updated")(pending)
  property("does not update the repository")(pending)
  property("does not give the updated book to the listener")(pending)
  property("does not give the original book to the listener")(pending)
}
