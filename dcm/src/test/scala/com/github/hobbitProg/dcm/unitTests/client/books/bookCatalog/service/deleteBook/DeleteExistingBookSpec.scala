package com.github.hobbitProg.dcm.unitTests.client.books.bookCatalog.service.deletBook

import org.scalatest.PropSpec

/**
  * Specification for deleting an existing book from the catalog using the
  * service
  * @author Kyle Cranmer
  * @since 0.3
  */
class DeleteExistingBookSpec
    extends PropSpec {
  property("indicates the catalog was updated")(pending)
  property("removes the book from the catalog")(pending)
  property("removes the book from the repository")(pending)
  property("gives the book to the listener")(pending)
}
