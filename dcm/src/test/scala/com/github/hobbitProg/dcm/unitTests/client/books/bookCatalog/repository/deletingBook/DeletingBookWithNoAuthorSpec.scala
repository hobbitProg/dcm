package com.github.dcm.unitTests.client.books.bookCatalog.repository.deletingBook

import org.scalatest.PropSpec

class DeletingBooksWithNoAuthorSpec
    extends PropSpec {
  property("the repository is marked as being unmodified")(pending)
  property("the database is unmodified")(pending)
}
