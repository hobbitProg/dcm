package com.github.hobbitProg.dcm.unitTests.client.cds.cdCatalog.model.addingCD

import org.scalatest.PropSpec

/**
  * Specificatgion for sucessfully adding new CDs to the catalog
  * @author Kyle Cranmer
  * @since 0.4
  */
class AddingCDSpec
    extends PropSpec {
  property("indicates the catalog was updated")(pending)
  property("places the CD into the catalog")(pending)
  property("gives new CD to all listeners")(pending)
}
