package com.github.hobbitProg.dcm.unitTests.client.cds.selectedCDView

import org.scalatest.FreeSpec

/**
  * Specification for clearing CD information on the selected CD view
  * @author Kyle Cranmer
  * @since 0.4
  */
class ClearSpec
    extends FreeSpec {
  "Given a selected CD control" - {
    "and a selected CD" - {
      "when the CD is cleared" - {
        "then there is no title displayed" in (pending)
        "and there is no artist displayed" in (pending)
        "and there is no ASIN displayed" in (pending)
        "and there is no cover displayed" in (pending)
        "and there are no categories displayed" in (pending)
      }
    }
  }
}
