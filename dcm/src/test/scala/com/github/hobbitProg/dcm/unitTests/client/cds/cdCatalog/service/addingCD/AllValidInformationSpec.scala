package com.github.hobbitProg.dcm.unitTests.client.cds.cdCatalog.addingCD

import org.scalatest.PropSpec

class AllValidInformationSpec
    extends PropSpec {
  property("indicates the CD was added to the catalog")(pending)
  property("places the CD into the catalog")(pending)
  property("places the CD into the repository")(pending)
}
