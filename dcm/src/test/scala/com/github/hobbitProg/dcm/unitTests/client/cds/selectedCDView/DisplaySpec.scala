package com.github.hobbitProg.dcm.unitTests.client.cds.selectedCDView

import org.scalatest.FreeSpec

/**
  * Specification for displaying CD information on the selected CD view
  * @author Kyle Cranmer
  * @since 0.4
  */
class DisplaySpec
    extends FreeSpec {
  "Given a selected CD control" - {
    "and a CD that is to be selected" - {
      "when the CD is selected" - {
        "then the CD's title is displayed" in (pending)
        "and the CD's artist is displayed" in (pending)
        "and the CD's ASIN is displayed" in (pending)
        "and the CD's cover is displayed" in (pending)
        "and the categories associated with the CD is displayed" in (pending)
      }
    }
  }
}
